export class RegistrationRequest {
  firebaseId: string;
  name: string;
  email: string;
  mobileNumber: string;
  profileUrl: string;
  fcmToken: string;
  deviceName: string;
  deviceId: string;
  provider: string;
}
